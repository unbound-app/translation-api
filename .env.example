PUBLIC_BASE_URL=https://translate.example.com
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_GUILD_ID=950850315601711176
DISCORD_REDIRECT_URI=https://translate.example.com/auth/callback

# openssl rand -base64 48
SESSION_JWT_SECRET=

# The values below are only used when running `bun run dev` directly on the
# host. docker-compose overrides PORT/REDIS_URL/LIBRETRANSLATE_URL for the
# containerized setup, so they can be left as-is either way.
PORT=47281
REDIS_URL=redis://localhost:6379
LIBRETRANSLATE_URL=http://localhost:5000
